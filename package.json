{
  "name": "tripgo-app",
  "version": "1.0.0",
  "description": "TripGo native app (Capacitor) — Android & iOS wrappers for the TripGo travel site.",
  "scripts": {
    "sync": "cap sync",
    "android:debug": "cap sync android && cd android && gradlew assembleDebug",
    "android:release": "cap sync android && cd android && gradlew assembleRelease",
    "ios:open": "cap sync ios && cap open ios"
  },
  "keywords": ["capacitor", "tripgo", "travel"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "@capacitor/ios": "^8.3.4"
  }
}
